name: pa11y tests

on:
  pull_request:

jobs:
  test:
    name: Pa11y Actions
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Pa11y Test
        uses: booleans-oss/pa11y-actions@v1
